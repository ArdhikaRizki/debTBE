# Database Connection
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:admin@localhost:5432/debt"

# JWT Secret Key
# Generate random key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="change-this-to-a-long-random-secret-key-for-production"

# Optional: JWT Expiration (default: 1d)
# JWT_EXPIRES_IN="7d"
